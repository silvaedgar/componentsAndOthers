1. Este proyecto controla el tiempo de inactividad con JavaScript para ello toma el tiempo del lifetime y le resta un tiempo para calcular cuanto le queda de inactividad al usuario. Pregunta si extiende el tiempo o no

2. Maneja excepciones se prueba en el crud de Usuarios al crear un registro si la BD retorna error(no se esta validando nada solo para efectos del uso de excepciones) llama la exception QueryExceptions, en otras excepciones llama la que esta en el app.php de bootstrap (por ser laravel 11). De esta manera no revienta el programa ya que llama la vista en exceptions\message

3. Habia un problema al llamar el cierre de sesion en el onclick puse ese nombre porque asi fue que funciono. Ni idea porque

4. El cierre automatico de sesion cree el middleware que esta alli en la carpeta de middelaware no lopude probar ya qu eno existe el kernel.php ni idea porque
    iba a actualizar el laravel con composer global update laravel/installer pero no lo hice por el banco no fuera a joder las aplicaciones (20-04-25)

5. Debo recordar que existe el metodo fieldsFiles en un trait de servicio que genera los campos de tabla solamente para grabar a partir de una variable(USERCONTROLLER)

Las claves son 12345678 o 123 o 123456

05/10/25
1. Siempre se llama el log de users_logs y si hay error se envia un parametro para que lo envie al archivo de storage/logs
2. Se incluyo validacion de los usuarios en el form con validate (debe probarse con form request)
3. Se quito el uso de queryexceptions


IMPORTANTE
Cuando se hace validate en el controlador la excepcion debe manejarse con         } catch (ValidationException $e) {  // el validate dentro de un try-catch no siugue el flujo normal
            $errores = implode(' | ', $e->validator->errors()->all()); // obtiene los errores
            // Y redirigir manualmente
            return redirect()->back()
                ->withErrors($e->validator)
                ->withInput();

Si se hace en el request la excepcion se puede quitar
